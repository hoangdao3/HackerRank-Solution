#include <stdio.h>
int main(){
    int n,name[1000][1000],phone[1000][1000];
    scanf("%d",&n);
    for(int i=1;i<=n;i++){
        fflush(stdin);
        scanf("%s",name[i]);
        scanf("%s",phone[i]);
    }
    int k=0;
    
    
}